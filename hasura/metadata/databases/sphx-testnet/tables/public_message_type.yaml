table:
  name: message_type
  schema: public
array_relationships:
  - name: messages
    using:
      foreign_key_constraint_on:
        column: type
        table:
          name: message
          schema: public
